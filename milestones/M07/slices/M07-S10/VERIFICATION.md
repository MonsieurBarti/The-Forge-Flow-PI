# Verification — M07-S10: Gap Features

| AC | Criterion | Verdict | Evidence |
|---|---|---|---|
| AC1 | Tool policies enforced before dispatch — ToolPolicyRule blocks disallowed tools | PASS | `ToolPolicyRule > blocks tools listed in defaults.blocked` (PASS); `ToolPolicyRule > allowed list restricts (whitelist behavior)` (PASS); `ToolPolicyRule > blocked and allowed interact: tool both blocked and not in allowed gets one violation` (PASS). 9/9 tests pass in `tool-policy.rule.spec.ts`. |
| AC2 | Per-tier and per-role rules composable via merge resolution | PASS | `ToolPolicyRule > merge chain: defaults -> byTier -> byRole` (PASS); `ToolPolicyRule > defaults.blocked accumulates with byRole.blocked` (PASS); `ToolPolicyRule > role-specific allowed overrides (security-auditor gets Read/Grep/Glob only)` (PASS); `ToolPolicyRule > byTier allowed narrows then byRole allowed overrides` (PASS). |
| AC3 | Tool policies configurable in settings.yaml under tool-policies | PASS | `ToolPoliciesConfigSchema > produces defaults from empty object` (PASS); `ToolPoliciesConfigSchema > accepts tool policies with defaults, byTier, and byRole` (PASS); `SettingsSchema new sections > includes toolPolicies defaults` (PASS). Schema confirmed: `SettingsSchema` includes `toolPolicies: ToolPoliciesConfigSchema.default(TOOL_POLICIES_DEFAULTS)` at line 356 of `project-settings.schemas.ts`. |
| AC4 | Stack detected automatically for Node/TS projects (package.json + tsconfig + lock file) | PASS | `DiscoverStackUseCase > detects Node/TS project from package.json with typescript dep and tsconfig` (PASS); `DiscoverStackUseCase > detects pnpm from pnpm-lock.yaml` (PASS); `DiscoverStackUseCase > detects npm from package-lock.json` (PASS); `DiscoverStackUseCase > detects yarn from yarn.lock` (PASS). 14/14 tests pass in `discover-stack.use-case.spec.ts`. |
| AC5 | Manual stack overrides in stack.overrides not clobbered by discovery | PASS | Schema design enforces this: `StackConfigSchema` has separate `detected` and `overrides` fields. `DiscoverStackUseCase` only returns `StackInfo` (populates `detected`), never touches `overrides`. Init test confirms: `expect(parsed.stack.overrides).toEqual({})` after discovery populates `detected` with framework/runtime/testRunner (line 175, `init-project.use-case.spec.ts`). |
| AC6 | Failure policy respected at phase transitions via guard context | PASS | `GuardContext` includes `failurePolicy: "strict"` (line 44, `orchestrate-phase-transition.use-case.spec.ts`). `Failure policy routing > strict mode returns trigger error unchanged (default behavior)` (PASS); failure policy is read from guard context and routed accordingly in all 6 failure policy tests. |
| AC7 | Tolerant mode records failures in journal without blocking (until retries exhausted) | PASS | `Failure policy routing > tolerant mode records FailureRecordedEntry then returns the error` (PASS). Test confirms: `appendSpy` called once with `entry.type === "failure-recorded"` and `metadata.action === "retried"`, then error is returned (not swallowed). |
| AC8 | Lenient mode allows phase transition despite non-critical failure (records + continues) | PASS | `Failure policy routing > lenient mode records FailureRecordedEntry and triggers success path` (PASS). Test confirms: invalid trigger "approve" on "discussing" fails, lenient mode records `metadata.action === "continued"`, then triggers success path via `PHASE_SUCCESS_TRIGGERS["discussing"] = "next"`, transitioning from "discussing" to "researching". |
| AC9 | Failure policies configurable in settings.yaml under workflow.failure-policies | PASS | `WorkflowConfigSchema > produces defaults from empty object` (PASS); `WorkflowConfigSchema > accepts failure policies with per-phase overrides` (PASS); `SettingsSchema new sections > includes workflow defaults` (PASS). Schema confirmed: `SettingsSchema` includes `workflow: WorkflowConfigSchema.default(WORKFLOW_DEFAULTS)` with `failurePolicies: BaseFailurePoliciesConfigSchema` containing `default` and `byPhase` fields (lines 183-189, `project-settings.schemas.ts`). |
| AC10 | Quality signals captured per stage via QualitySnapshot at phase boundaries | PASS | `OrchestratePhaseTransitionUseCase > writes quality snapshot when metricsRepository provided` (PASS). Test confirms: after phase transition from "discussing" to "researching", `metricsRepo.readQualitySnapshots(sliceId)` returns 1 snapshot with `sliceId`, `milestoneId`, and default metric values (testsPassed=0, testsFailed=0, lintErrors=0, typeErrors=0). |
| AC11 | Trends queryable by milestone/slice via MetricsQueryPort phase methods | PASS | `AggregateMetricsUseCase > aggregateByPhase groups metrics by phase` (PASS). Test confirms: 3 entries (2 executing, 1 verifying) correctly grouped into `byPhase` record with per-phase taskCount, successCount, failureCount, totalCostUsd. `aggregateByPhase uses 'unknown' for entries without phase` (PASS). 6/6 tests pass. |
| AC12 | tff init triggers stack discovery and populates stack.detected in settings | PASS | `InitProjectUseCase > populates stack.detected in settings.yaml when discoverStack is provided` (PASS). Test confirms: `DiscoverStackUseCase` injected into `InitProjectUseCase`, after execution `parsed.stack.detected.framework === "next"`, `runtime === "typescript"`, `testRunner === "vitest"`, and `overrides` remains empty. `InitProjectUseCase > writes settings.yaml without stack.detected when discoverStack is not provided` (PASS) confirms backward compat. |

## Summary
12/12 PASS, 0/12 FAIL
